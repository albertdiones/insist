{
    "name": "insist",
    "version": "0.0.0",
    "main": "index.ts",
    "type": "module",
    "scripts": {
        "test": "bun test --timeout=20000",
        "validate-ts": "tsc --noEmit --pretty --skipLibCheck"
    },
    "devDependencies": {
        "@types/bun": "latest",
        "jest": "^29.7.0"
    },
    "peerDependencies": {
        "typescript": "^5.6.3"
    },
    "dependencies": {
        "add_repeater": "^0.0"
    }
}
